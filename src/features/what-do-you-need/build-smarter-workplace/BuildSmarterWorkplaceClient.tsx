"use client";

// Placeholder page — real content pending. `Navbar` and `Footer` already wrap
// every route from `src/app/[locale]/layout.tsx`, so this renders only the body.

export function BuildSmarterWorkplaceClient() {
  return <div>This is Build a Smarter Workplace page</div>;
}
